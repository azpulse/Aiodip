name: dj_aiodip
description: DJ Aiodip — mix, edit, and record sets (App Store / Play Store)
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  supabase_flutter: ^2.9.1
  shared_preferences: ^2.5.3
  file_picker: ^10.2.0
  path: ^1.9.1
  uuid: ^4.5.1
  intl: ^0.20.2
  url_launcher: ^6.3.1
  in_app_purchase: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
